import React from "react";
import Layout from "../../layout";

const Day1Page = (props) => {
  const {
    day,
    totalBalance,
    familyStrike,
    healthStrike,
    workStrike,
    socialStrike,
  } = props;
  return (
    <Layout>
      <p>Question1</p>
    </Layout>
  );
};

export default Day1Page;
